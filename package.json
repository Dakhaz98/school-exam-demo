{
  "name": "school-exam-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Trial school exam platform with Excel roster, rooms, and lobby timing.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "doctor": "node scripts/doctor.js",
    "web:dev": "npm run dev --prefix web",
    "web:build": "npm run build --prefix web",
    "web:start": "npm run start --prefix web"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.2",
    "socket.io": "^4.8.1",
    "xlsx": "^0.18.5"
  }
}
